# 📄 Python CSV Module – Beginner's Guide

The **`csv` module** is built into Python (no extra installation required) and is used to **read from** and **write to** CSV (Comma-Separated Values) files.

```python
import csv
